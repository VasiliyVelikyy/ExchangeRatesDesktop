<Window x:Class="ExchangeRates.View.WindowRates"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ExchangeRates.View"
        mc:Ignorable="d"
        Title="Rates" Height="300" Width="300">
    <StackPanel Background="#FFD8D8D8">
        
        <StackPanel Orientation="Horizontal" Background="#FFE4E0E0">
            <Button Style="{StaticResource ButtonMenu}" Content="Добавить" Command="{Binding AddRate}" />
        </StackPanel>

        <Label Margin="5" HorizontalAlignment="Center">Список доступных валют
        </Label>

        <ListView ItemsSource="{Binding ListRates}"
            x:Name="lvRole" Background="#FFD3CDCD">
            <ListView.View >
                <GridView>
                    <GridView.Columns>
                        <GridViewColumn Header="Код" Width="50" DisplayMemberBinding="{Binding Id}"/>
                        <GridViewColumn Header="Наименование Валюты" DisplayMemberBinding="{Binding NameRate}"/>
                    </GridView.Columns>
                </GridView>
            </ListView.View>
        </ListView>

    </StackPanel>
</Window>
